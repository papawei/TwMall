'use strict';

import {StatusBarHeight, NavigatorBarHeight, ColorMap, FontSizeMap, BaseStyles} from './styles/BaseStyles'

module.exports = {
    StatusBarHeight,
    NavigatorBarHeight,
    ColorMap,
    FontSizeMap,
    BaseStyles
}